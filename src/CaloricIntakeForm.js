import 'bootstrap/dist/css/bootstrap.min.css';
import { Form } from 'react-bootstrap'

function CaloricIntakeForm() {
    return (
        <Form  className="MealForm">
            {/*ToDo Form for calculating caloric intake */}
        </Form>
    );
}
export default CaloricIntakeForm;